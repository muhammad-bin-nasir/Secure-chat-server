# Railway Environment Variables Example
# Copy this to .env for local development

# MongoDB Configuration (Standard MongoDB)
MONGODB_HOST=localhost
MONGODB_PORT=27017
MONGODB_DB=secure_chat
MONGODB_USERNAME=
MONGODB_PASSWORD=

# For Railway deployment with MongoDB service:
# MONGODB_HOST=mongodb-service-name
# MONGODB_USERNAME=your_username
# MONGODB_PASSWORD=your_password

# Server Configuration (Railway will set these automatically)
PORT=5000
RAILWAY_STATIC_URL=
RAILWAY_PUBLIC_DOMAIN=

# Optional: Add your own environment variables
# MAX_CLIENTS=50
# BUFFER_SIZE=16384
